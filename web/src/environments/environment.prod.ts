// The plain "lead-tracker-api" name collided on Render, so it was
// assigned this suffixed URL instead - confirmed against the real
// deployed service, not the name declared in render.yaml.
export const environment = {
  production: true,
  apiUrl: 'https://lead-tracker-api-p27s.onrender.com/api',
};
